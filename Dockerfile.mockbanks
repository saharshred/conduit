FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod ./
COPY . .
RUN go build -o /out/mockbanks ./cmd/mockbanks

FROM alpine:3.20
COPY --from=build /out/mockbanks /usr/local/bin/mockbanks
COPY data/ /data/
ENTRYPOINT ["/usr/local/bin/mockbanks", "/data/"]
